@page "/tiposconta/edit/{Id:int}"
@using WebJar.Frontend.Shared

<h3>Editar Tipo documento contable</h3>

@if (tipoConta is null)
{
<Loading/>
}
else
{
<TiposContaForm @ref="tiposContaForm" TipoConta="tipoConta" OnValidSubmit="EditAsync" ReturnAction="Return" />
}